import AppHeader from './index';

import users from 'lib/mocks/users';
import links from 'lib/mocks/links';

import { Logo } from 'components';

<Meta
  title="organism/AppHeader"
  args={{
    appName: 'Builda - The Everything Generator',
    logo: <Logo />,
    links: links
  }}
/>

# App header

<Description of={AppHeader} />

<Canvas withSource="open" withToolbar>
  <Story name="Logged out">{(args) => <AppHeader {...args} />}</Story>
</Canvas>

<Canvas withToolbar>
  <Story name="Logged in">
    {(args) => <AppHeader {...args} user={users[0]} />}
  </Story>
</Canvas>
